Read  Two queries in a single transaction T1 execute a SELECT but get different values because another transaction T2 has changed data and COMMITTED  Phantom Reads  When a transaction T1 is running and another transaction T2 adds or deletes rows from the set T1 is using Example Transaction  Transfer Money DELIMITER  CREATE PROCEDURE transfer IN senderid INT IN receiverid INT IN amount DECIMAL102 BEGIN DECLARE rollbackmessage VARCHAR255 DEFAULT Transaction rolled back Insufficient funds DECLARE commitmessage VARCHAR255 DEFAULT Transaction committed successfully  Start the transaction START TRANSACTION  Attempt to debit money from account 1 UPDATE accounts SET balance  balance  amount WHERE accountid  senderid  Attempt to credit money to account 2 UPDATE accounts SET balance  balance  amount WHERE accountid  receiverid  Check if there are sufficient funds in account 1 IF SELECT balance FROM accounts WHERE accountid  senderid  0 THEN  Roll back the transaction if there are insufficient