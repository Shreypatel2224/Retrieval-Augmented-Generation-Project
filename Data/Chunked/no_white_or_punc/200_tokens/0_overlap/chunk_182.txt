we follow the pointer to the left of 42  Note None of these new values cause a node to split B Tree m  4  Insert 30  Starting at the root we descend to the leftmost child we’ll call it curr  curr is