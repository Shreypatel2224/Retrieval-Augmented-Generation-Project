or a small multiple of the disk block size If the next record to be inserted belongs to a chunk that hasn’t filled its block then we can just insert it there The fact that this might cause other records in that chunk to move a little bit in the array is not important since this does not cause any extra disk accesses so long as we move data within that chunk But what if the chunk fills up the entire block that contains it We could just split it in half What if we want to delete a record We could