internal node The third insertion propagates all the way to the root Deletion works in the opposite way the element is removed from the leaf If the leaf becomes empty a key is removed from the parent node If that breaks invariant 3 the keys of the parent node and its immediate right or left sibling are reapportioned among them so that invariant 3 is satisfied If this is not possible the parent node can be combined with that sibling removing a key another level up in the tree and possible