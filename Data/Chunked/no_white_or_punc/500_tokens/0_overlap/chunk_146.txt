consider whether its even worth our time What can we deduce about the cost of maintaining completeness even if we havent figured out an algorithm yet One example demonstrates a very big problem Suppose we had the binary search tree on the left — which is complete by our definition — and we wanted to insert the key 1 into it If so we would need an algorithm that would transform the tree on the left into the tree on the right31425 439 PM ICS 46 Spring 2022 Notes and Examples AVL Trees