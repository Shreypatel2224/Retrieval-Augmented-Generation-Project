room in the node the new element can be inserted simply Otherwise the current leaf is already full and must be split into two leaves one of which acquires the new element The parent is then updated to contain a new key and child pointer If the parent is already full the process ripples upwards eventually possibly reaching the root If the root is split into two then a new root is created with just two children increasing the height of the tree by one For example here is the effect of a series of