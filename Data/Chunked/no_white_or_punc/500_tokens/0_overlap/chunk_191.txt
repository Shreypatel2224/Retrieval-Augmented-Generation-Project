mutually consistent  Eventual Consistency  The system will eventually become consistent  All writes will eventually stop so all nodesreplicas can be updated Categories of NoSQL DBs  Review KeyValue Databases KeyValue Stores  Key  Value  Designed around  Simplicity The data model is extremely simple Comparatively tables in an RDBMS are very complex Lends itself to simple CRUD operations and API creation  Speed Usually deployed as inmemory DB Retrieving a value given its key is typically an O1