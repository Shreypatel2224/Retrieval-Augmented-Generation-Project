difficult to implement correctly For insertion we first find the appropriate leaf node into which the inserted element falls assuming it is not already in the tree If there is already room in the node the new element can be inserted simply Otherwise the current leaf is already full and must be split into two leaves one of which acquires the new element The parent is then updated to contain a new key and child pointer If the parent is already full the process ripples upwards eventually possibly