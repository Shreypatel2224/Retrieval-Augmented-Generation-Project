at least to a certain minimum percentage This improves space efficiency while reducing the typical number of disk fetches necessary during a search or update operation A Btree of order is defined to have the following shape properties The root is either a leaf or has at least two children Each internal node except for the root has between and children All leaves are at the same level in the tree so the tree is always height balanced The Btree is a generalization of the 23 tree Put another way a