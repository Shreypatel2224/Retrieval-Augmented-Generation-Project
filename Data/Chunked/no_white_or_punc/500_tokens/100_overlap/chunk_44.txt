at most 100 million records Thus it would require an extremely large database to generate a tree of more than height four The tree split and insert rules guarantee that every node except perhaps the root is at least half full So they are on average about 34 full But the internal nodes are purely overhead since the keys stored there are used only by the tree to direct search rather than store actual data Does this overhead amount to a significant use of space No because once again the high